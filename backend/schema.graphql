type Task{
  id: Int!
  name: String
  description: String
}
type User{
  id: Int!
  tokenId: String!
  name: String
}
type Author {
  id: ID
  name: String
}
type Book {
  title: String
  author: String
}
type Query {
  getAllBooks: [Book!]!
  getAuthor(id: Int!): Author
  hello: String
  getUser(id: Int!): User
}